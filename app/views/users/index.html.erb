<% provide(:title, 'All users') %>

<% if current_user && current_user.admin? %>
<h3>All users</h3>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= render user %>
    </li>
  <% end %>
<% end %>
</ul>
